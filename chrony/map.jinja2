{% import_yaml 'chrony/defaults.yml' as defaults %}

{% set chrony = salt.grains.filter_by({
  'Debian': {
    'service_name': 'chrony',
  },
  'default': defaults, 
}, merge=salt.pillar.get('chrony'), base='default') %}
